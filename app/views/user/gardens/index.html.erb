


<div class="container" style="margin-top: 20px; margin-bottom: 20px;">
  <div class="row panel">
    <div class="col-md-4">
          <img src="https://cdn4.iconfinder.com/data/icons/social-messaging-ui-color-and-shapes-3/177800/129-512.png" class="img-thumbnail picture hidden-xs">
    </div>
        <div class="col-md-8  col-xs-12 border:1px green solid" >
          <div class="col-md-4">
            <h1>My Profile</h1>
          </div>
           <div class="header">
                <h4> USER NAME : </h4>
                <p>some name</p>
                <h4>USER EMAIL :</h4>
                <p>some email</p>
                <h4>USER DESCRIPTION :</h4>
                <p>huge description about his gardes, where it is , what he is searching for </p>
    </div>
           </div>
        <div class="col-md-4">
          <%= link_to 'Upload Photo', "edit_garden_path(garden)", class: "btn btn-outline-success" %>
        </div>
        <%= link_to 'Edit Profile', "edit_garden_path(garden)", class: "btn btn-outline-success" %>
        <%= link_to 'Delete Profile', "garden_path(garden)", method: :delete, data: { confirm: 'Are you sure you want to delete your profile?' }, class: "btn btn-outline-success"  %>
    </div>
        </div>
    </div>
</div>
</div>
<div class="container w-50">
  <br>
  <% @gardens.each do |garden| %>
  <div class="card-trip">
    <% if garden.photo.nil? %>
    <img src="https://images.unsplash.com/photo-1516253593875-bd7ba052fbc5?ixlib=rb-1.2.1&ixid=eyJhcHBfaWQiOjEyMDd9&auto=format&fit=crop&w=1650&q=80" />
    <% else %><%= image_tag garden.photo %>
    <% end %>

  <div class="card-trip-infos">
    <div>
      <h2><%= garden.name%></h2>
      <p><%= garden.address %></p>
      <p><%= garden.price_per_day %>€ per day</p>
      <p><%= garden.description %></p>
    </div>
    <div>
      <%= link_to 'Edit', edit_garden_path(garden), class: "btn btn-outline-success" %>
      <%= link_to 'Delete', garden_path(garden), method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-outline-success"  %>
    </div>
  </div>
</div>
</br>
<%end%>


<% if @garden.nil?%>
<p>You have no gardens</p>
        <%=link_to 'Home Page', root_path, class: 'btn btn-outline-success' %>
      <% else%>
        <%= link_to 'Back', gardens_path, class: "btn btn-outline-success" %>
      <% end %>
</div>
